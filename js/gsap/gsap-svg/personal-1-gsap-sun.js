const gsapSun = () => {

    /* document.addEventListener('DOMContentLoaded', () => {
        let sunTimeline = gsap.timeline();

        sunTimeline.to(".sun", {
            x: "15%", 
            y: "15%", 
            duration: 10,
        });
    });
    */
   console.log('sun')
};

export default gsapSun;